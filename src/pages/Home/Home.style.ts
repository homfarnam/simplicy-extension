import styled from "styled-components"

const Subtitle = styled.p`
  font-style: normal;
  font-weight: bold;
  font-size: 30px;
  line-height: 35px;
  color: #213445;
  display: flex;
  justify-content: center;
`

const GetStarted = styled.button`
  background: #213445;
  color: white;
  width: 220px;
  height: 60px;
`

const Header = styled.header`
  font-family: Helvetica;
`

export { GetStarted, Header, Subtitle }
